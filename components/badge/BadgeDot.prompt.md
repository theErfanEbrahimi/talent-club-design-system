The small dot rendered inside a badge's `dot` icon slot (Figma `1:2`, 6 variants). Standalone status dots should use `Dot` instead.

```jsx
<BadgeDot size="md" />
<BadgeDot size="lg" outline />
```

`size` `sm|md|lg`; `outline` adds the halo ring.

The unstyled shell behind `Badge` — reach for `Badge` first; use `BadgeBase` only when you need the icon-only variant or a custom child.

```jsx
<BadgeBase icon="only" />
<BadgeBase size="md" color="success">۹۸٪</BadgeBase>
```

Same `size` / `color` / `type` / `icon` vocabulary as `Badge`.

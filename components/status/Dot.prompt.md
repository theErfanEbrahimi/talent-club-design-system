Status dot — a small colored circle for status indicators and inside badges.

```jsx
<Dot color="success" />
<Dot color="error" outline />
```

Sizes `sm` `md` `lg`; colors map to the semantic ramps (`success`, `error`, `warning`, `brand`, `blue`, `violet`, `gray`). `outline` adds a translucent ring.
